#include <stdio.h>

main(){
	char mor[12];
	int p,age,t;
	printf("entre ton morphologie  :");
	scanf("%s",&mor);
	printf("entre ton age  :");
	scanf("%d",&age);
		printf("entre ton   :");
	scanf("%s",&mor);
}
